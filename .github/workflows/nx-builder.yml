name: Docker Builder

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/nx-builder.yml
      - docker/docker-entrypoint.sh
  pull_request:
    paths:
      - .github/workflows/nx-builder.yml
      - docker/docker-entrypoint.sh

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        NODE_VERSION:
          - '12.22.10'
          - '14.19.0'
          - '16.14.0'
          - '17.6.0'
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - run: echo "NODE_VERSION=${{ matrix.NODE_VERSION }}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1.6.0
        with:
          version: v0.8.0

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v3.6.2
        with:
          images: ghcr.io/gperdomor/nx-docker
          flavor: |
            latest=${{ matrix.NODE_VERSION == '16.14.0' }}
            suffix=-alpine
          tags: |
            type=semver,pattern={{version}},value=${{ matrix.NODE_VERSION }}
            type=semver,pattern={{major}}.{{minor}},value=${{ matrix.NODE_VERSION }}
            type=semver,pattern={{major}},value=${{ matrix.NODE_VERSION }}
          labels: |
            org.opencontainers.image.authors=Gustavo Perdomo <gperdomor@gmail.com>
            org.opencontainers.image.description=Builder companion for @nx-tools/nx-docker npm package

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1.14.1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2.10.0
        with:
          context: ./docker
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=ghcr.io/gperdomor/nx-docker:${{ matrix.NODE_VERSION }}-buildcache
          cache-to: type=registry,ref=ghcr.io/gperdomor/nx-docker:${{ matrix.NODE_VERSION }}-buildcache,mode=max
          build-args: |
            NODE_VERSION=${{ matrix.NODE_VERSION }}
            ALPINE_VERSION=${{ env.ALPINE_VERSION }}

      - name: Image ditagsgest
        run: echo "${{ steps.docker_build.outputs.digest }}"
