# class-variance-builder

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build class-variance-builder` to build the library.

## Running unit tests

Run `nx test class-variance-builder` to execute the unit tests via [Vitest](https://vitest.dev/).
