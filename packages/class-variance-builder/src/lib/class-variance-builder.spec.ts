import { classVarianceBuilder } from './class-variance-builder.js';

describe('classVarianceBuilder', () => {
  it('should work', () => {
    expect(classVarianceBuilder()).toEqual('class-variance-builder');
  });
});
