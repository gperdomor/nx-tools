{
  "$schema": "https://json-schema.org/draft-07/schema",
  "cli": "nx",
  "$id": "https://json-schema.org/draft-07/schema",
  "title": "Prisma Rollback executor",
  "description": "",
  "type": "object",
  "properties": {
    "schema": {
      "type": "string",
      "description": "The path to the prisma schema eg. 'prisma/schema.prisma'"
    },
    "silent": {
      "type": "boolean",
      "description": "Remove all terminal outputs the child process"
    }
  },
  "required": ["schema"]
}
