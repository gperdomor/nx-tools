{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Prisma Status executor",
  "description": "Looks up the migrations in /prisma/migrations/* folder and the entries in the _prisma_migrations table and compiles information about the state of the migrations in your database.",
  "type": "object",
  "properties": {
    "schema": {
      "type": "string",
      "description": "The path to the prisma schema eg. 'prisma/schema.prisma'"
    }
  },
  "required": []
}
