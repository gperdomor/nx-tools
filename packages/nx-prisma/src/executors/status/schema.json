{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Prisma Status executor",
  "description": "Check the status of your database migrations.",
  "type": "object",
  "properties": {
    "schema": {
      "type": "string",
      "description": "The path to the prisma schema eg. 'prisma/schema.prisma'"
    }
  },
  "required": []
}
